package com.example.productgallery.ui.navigation

enum class AppDestination(val route: String, val label: String) {
    Gallery("gallery", "Gallery"),
    Requests("requests", "Requests"),
    Import("import", "Import")
}
